import { createTheme } from '@mui/material/styles';

export const theme = createTheme({
  palette: {
    primary: {
      main: '#FF7A82',
      contrastText: '#fff',
    },
    secondary: {
      main: '#868AE0',
      contrastText: '#fff',
    },
    tertiary: {
      main: '#4DAF7B',
      contrastText: '#fff',  
    }
  },
});
